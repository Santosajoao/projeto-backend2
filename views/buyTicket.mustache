<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Comprar Ticket</title>
    <style>
        body {
            font-family: Arial, sans-serif;
           
            height: 100vh;
            background-color: #f4f4f4;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        input, button {
            width: 100%;
            padding: 10px 0px;
            margin: 10px 0;
        }
        button {
            background: #007BFF;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover {
            background: #0056b3;
        }
        .register-link {
            display: block;
            margin-top: 10px;
            text-decoration: none;
            color: #007BFF;
        }
        .register-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h2>Comprar Ticket</h2>
    
    
    <form action="/tickets/buyTicket" method="POST">
       <label for="id">Id do ticket:</label>
            <input type="text" id="id" name="ticketId" required>

            <label for="quantity">Quantidade:</label>
            <input type="text" id="quantity" name="quantity" required>

            <button type="submit">Comprar</button>
    </form>
    
    <a href="/tickets">Cancelar</a>
</body>
</html>
